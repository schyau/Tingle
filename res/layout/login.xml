<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundC"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <TextView
        android:id="@+id/login_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="Tingle"
        android:textColor="@color/textC"
        android:textSize="75sp" />

    <FrameLayout
        android:id="@+id/passwordFL"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingBottom="15dp"
        android:paddingTop="15dp" >

        <EditText
            android:id="@+id/password"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/inputboxC"
            android:hint="Password"
            android:inputType="textPassword"
            android:padding="5dp"
            android:singleLine="true"
            android:textColor="@color/textC"
            android:textColorHint="@color/userpassC" />
    </FrameLayout>

    <FrameLayout
        android:id="@+id/userNameFL"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/passwordFL"
        android:layout_centerInParent="true" >

        <EditText
            android:id="@+id/username"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/inputboxC"
            android:hint="Username"
            android:padding="5dp"
            android:singleLine="true"
            android:textColor="@color/textC"
            android:textColorHint="@color/userpassC" />
    </FrameLayout>

    <FrameLayout
        android:id="@+id/loginFL"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/passwordFL"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5dp" >

        <TextView
            android:id="@+id/login"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/buttonC"
            android:gravity="center"
            android:padding="5dp"
            android:text="Tap to start Tinglin&apos;!"
            android:textColor="@color/buttontextC"
            android:textSize="20sp" />
    </FrameLayout>

    <TextView
        android:id="@+id/create_new_account"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/loginFL"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:padding="5dp"
        android:text="Create New Account"
        android:textColor="@color/buttontextC"
        android:textSize="20sp" />

    <ImageView
        android:id="@+id/login_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/tinglenotificationicon"
        android:visibility="gone" />

</RelativeLayout>